# lib_a/lib_aConfig.cmake.in
# This file is generated during installation and helps find_package(lib_a) locate the library.

# Provide the installed targets
include("${CMAKE_CURRENT_LIST_DIR}/lib_a_targets.cmake")

# Set the public include directories for consumers of lib_a
set(lib_a_INCLUDE_DIRS "${CMAKE_INSTALL_PREFIX}/include")

# Define variables that indicate the library was found
set(lib_a_FOUND TRUE)
